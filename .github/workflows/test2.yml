name: Test Runner

on: [push]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Проверка запуска
        run: echo "✅ Runner работает на сервере!"
      
      - name: Создаём тестовый файл
        run: touch ~/test-runner-file.txt
